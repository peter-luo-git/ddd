# 微服务拆分设计工具 —— 项目文档与 AI 智能审查赛题

> 本文包含：项目功能与架构简介、部署与运行方法、使用方法，以及本次赛题核心
> ——**基于 AI、依据微服务设计原则对拆分方案进行合理性智能审查。赛题本身目前只需要使用到C4架构设计。**

---

## 一、项目简介

本工具是一个**面向微服务拆分设计的可视化建模平台**，帮助团队把「业务分析 → 战略设计 →
架构设计 → 数据/代码建模」一条链路做在一个工具里，并产出**结构化、机器可读的模型**，
供 AI 对拆分方案的合理性做智能审查。

设计取向：**聚焦"微服务拆分设计"，简单上手、避免复杂 UML 全建模**；模型带标准化
元数据/标签，天然适合喂给 AI 评审。

---

## 二、功能介绍

### 2.1 多视图工作台（三大阶段）
在同一个项目下按阶段切换视图，前一阶段的产出是后一阶段的输入：

| 阶段 | 子视图 | 作用 |
|------|--------|------|
| **业务分析** | 事件风暴 | 用领域事件/命令/聚合/策略/读模型/热点发现业务流程与**边界**（聚合 = 候选微服务） |
| **业务分析** | 领域故事 | 角色 + 工作对象 + **带编号的动词箭头**，叙事式讲清"谁、用什么、做什么" |
| **战略设计** | 限界上下文 | 逐个限界上下文（≈ 微服务）结构化记录（Bounded Context Canvas） |
| **架构设计** | C4 架构设计 | 微服务拆分主战场：C4 四层下钻建模 |

### 2.2 C4 四层下钻（架构设计核心）
`软件系统 ⊃ 容器（微服务） ⊃ 组件 ⊃ 代码`，双击下钻、面包屑返回；**一份模型、多层视图**，
切换层级只过滤视图、不丢数据。

- **微服务/基础设施元素**：微服务(容器)、API 网关、消息中间件、数据库、文件/对象存储、
  缓存、Saga 编排器、外部系统、用户/角色。
- **关系**：同步(REST/gRPC)、异步(发布订阅)、数据依赖、编排；线型随通信方式自动联动。

### 2.3 数据建模与 UML（组件/代码层）
- **数据实体（ER 表）**：列含 **PK/FK/类型/可空**；实体间关系用 **Crow's Foot 乌鸦脚**表示基数
  (1:1 / 1:N / N:M …)。
- **UML 类**：标准三分栏（类名 / 属性 / 操作），属性含可见性与类型，操作含参数与返回值。
- 字段编辑采用**下拉选关键字**（可见性 `+ - # ~`、PK/FK、常用类型），无需记语法。

### 2.4 标准化元数据 / 标签（AI 审查的结构化输入）
- **元素元数据**：子域类型(core/supporting/generic)、状态性(stateful/stateless)。
- **关系元数据**：通信方式(sync/async/pub-sub/data/orchestration)、一致性(strong/eventual)、基数。
- **受控标签词表**：
  - 子域：`core` / `supporting` / `generic`
  - 通信：`sync` / `async` / `pub-sub`
  - 一致性：`strong` / `eventual`
  - DDD：`aggregate-root` / `anti-corruption-layer` / `open-host-service` / `published-language`
  - 风险：`shared-database` / `chatty` / `god-service` / `anemic-model`

### 2.5 导入导出
JSON（本工具原生）、DSL 文本、Structurizr、PlantUML、Mermaid、Draw.io、PNG/SVG。
其中 **JSON / DSL 是 AI 审查的主输入**。

### 2.6 项目管理（后端）
项目新建 / 保存 / 打开 / 删除，模型存于 SQLite；结构校验、导出、设计词表接口。

---

## 三、技术架构

```
┌──────────────────────── 前端 (bac4-standalone) ────────────────────────┐
│  React 19 + React Flow 12 + Zustand + Vite + Tailwind CSS              │
│  · 多视图工作台（事件风暴/领域故事/限界上下文/C4）                     │
│  · 元素类型注册表 src/config/elementTypes.js（单一事实源，加元素只改它）│
│  · 单一 elements 数组 + 多视图 boards 快照                             │
│  · 模型可导出 JSON / DSL / Structurizr / PlantUML / Mermaid / Draw.io  │
└───────────────────────────────┬────────────────────────────────────────┘
                                 │ REST / JSON（CORS 开放）
┌───────────────────────────────▼────────────────────────────────────────┐
│                    后端 (backend, FastAPI)                             │
│  · 项目 CRUD + 模型持久化（SQLite + SQLAlchemy）                       │
│  · 模型结构校验（重复 id / 未知类型 / 孤儿关系）                        │
│  · 导出（JSON / DSL 文本）  · 设计词表 /api/vocab                      │
│  · AI 审查接口 /review（占位，返回 501，留给选手实现）                 │
└─────────────────────────────────────────────────────────────────────────┘
```

**目录结构（关键）**
```
ddd/
├── bac4-standalone/            # 前端
│   ├── src/config/elementTypes.js   # 元素/元数据/标签 注册表（单一事实源）
│   ├── src/config/views.js          # 多视图工作台阶段与视图
│   ├── src/store.js                 # Zustand：多视图 boards、下钻、导入导出
│   ├── src/components/              # 画布、工具栏、属性面板、字段编辑器、导航等
│   └── src/examples/                # 内置示例（在线书店等）
├── backend/                    # 后端 FastAPI + SQLite
│   └── app/{main,dsl,vocab,models,database}.py
├── notes/                      # 设计蓝本与说明
└── 项目文档与AI审查赛题.md      # 本文
```

---

## 四、部署与运行

### 环境要求
- Node.js 18+（前端）
- Python 3.10+（后端）

### 前端
```bash
cd bac4-standalone
npm install
npm run dev          # 默认 http://localhost:5173
```
> 前端可独立使用（画布、示例、导入导出、本地自动保存都在浏览器内），不依赖后端。

### 后端（可选，用于项目保存/打开与 AI 审查）
```bash
cd backend
python3 -m venv .venv
./.venv/bin/pip install -r requirements.txt
./.venv/bin/uvicorn app.main:app --host 127.0.0.1 --port 8000
```
- 交互式接口文档：http://127.0.0.1:8000/docs
- 健康检查：http://127.0.0.1:8000/api/health
- 数据库文件 `backend/data.db` 首次启动自动创建。

---

## 五、使用方法

### 推荐建模流程（自上而下）
1. **业务分析 · 事件风暴**：铺领域事件(橙)→命令(蓝)→聚合(黄)→策略/读模型，识别业务边界。
2. **业务分析 · 领域故事**：用角色+工作对象+编号箭头把关键业务流程讲一遍。
3. **战略设计 · 限界上下文**：为每个候选上下文填 Bounded Context Canvas（职责、语言、通信、决策）。
4. **架构设计 · C4**：
   - 上下文层建"软件系统"；双击进入 → 容器层拖微服务与基础设施（网关/中间件/DB/缓存…）并连线；
   - 给微服务标**子域类型/状态性**，给关系标**通信方式/一致性**（这些是 AI 审查依据）；
   - 双击微服务 → 组件层建组件与**数据实体(ER 表)**；双击组件 → 代码层建 **UML 类**。
5. **导出**：JSON / DSL，作为 AI 审查输入。

### 关键交互
- 拖拽添加元素；单击编辑属性；**双击下钻**；面包屑返回上层。
- 关系设"通信方式"→线型自动联动；数据实体间设"基数"→显示乌鸦脚。

---

## 六、赛题：基于 AI 的微服务拆分智能审查（本次核心，赛题本身目前只需要使用到C4架构设计）

> **底座已就绪但不实现审查逻辑**：已落地标准化元数据/标签、模型可导出、并预留审查接口
> `POST /api/projects/{id}/review`（当前返回 501）。**选手需实现该审查能力。**

### 6.1 任务目标
输入一份微服务拆分模型（JSON/DSL），由 AI **依据微服务设计原则**判断拆分的合理性，
输出**结构化审查报告**：逐条问题 + 严重度 + 定位(元素/关系) + 依据 + 改进建议。

### 6.2 输入 / 输出契约

**输入**：本工具导出的模型 JSON（分组结构），要点字段：
- 元素：`id, type, name, technology, tags[], meta{subdomainType, stateful},
  parentSystem/parentContainer/parentComponent, attributes[], methods[]`
- 关系：`from, to, description, meta{kind, consistency}, cardinality, technology`

**建议输出**：
```json
{
  "score": 0-100,
  "summary": "总体评价",
  "issues": [
    {
      "dimension": "耦合/内聚|边界|数据所有权|通信|一致性|反模式|命名|可演进性",
      "severity": "high|medium|low",
      "target": { "kind": "element|relationship", "id": "svc-order" },
      "message": "问题描述",
      "evidence": "依据的模型字段/结构",
      "suggestion": "改进/重构建议"
    }
  ]
}
```

### 6.3 微服务设计原则与审查维度（评审规格）

以下是 AI 应据以判断拆分合理性的原则与**可检查点**，并标注**依据的模型证据**。

**1. 围绕业务能力/子域拆分（Single Responsibility / DDD 对齐）**
- 每个微服务应对应一个业务能力或一个限界上下文，职责单一。
- 检查：`meta.subdomainType` 是否标注；核心域是否被拆散或与支撑/通用域混在一个服务；
  一个服务是否承担过多不相关职责（名称/关系过杂 → god-service）。
- 依据：subdomainType、服务的入/出关系数量、组件构成。

**2. 高内聚、低耦合（拆分的核心度量）**
- 服务内元素紧密相关（高内聚）、服务间依赖尽量少（低耦合）。
- 检查：跨服务同步调用数量、扇入/扇出、是否存在**循环依赖**、调用链深度（chatty）。
- 依据：relationships 的 from/to 拓扑、`meta.kind=sync` 的密度。

**3. 边界与聚合一致性**
- 一个聚合是一个强一致性边界，不应横跨服务；服务边界不应切断需强一致的事务。
- 检查：`consistency=strong` 的关系是否跨服务；聚合是否被拆到多个服务。
- 依据：关系 `meta.consistency`、组件/实体的 parent 归属。

**4. 数据库独享 / 数据所有权（Database per Service）**
- 每个服务独享自己的数据存储，不共享数据库、不直接读别人的库。
- 检查：是否多个微服务指向同一 `database`（共享库反模式）；是否存在服务直接读取
  他服务数据库的数据依赖。
- 依据：指向同一 database 节点的关系；`tags` 含 `shared-database`。

**5. 通信方式合理性（同步 vs 异步）**
- 优先通过 API 网关统一入口；服务间跨边界优先**异步/事件驱动**解耦，避免长同步链路。
- 检查：本应异步却用同步（强耦合/级联失败风险）；无网关直连内部服务；消息中间件缺失。
- 依据：关系 `meta.kind`、是否存在 apiGateway / messageBroker 节点、`tags` 含 `chatty`。

**6. 分布式一致性与事务（Saga / 最终一致）**
- 跨服务事务应用 Saga/最终一致，避免跨服务两阶段强一致。
- 检查：跨服务 `strong` 一致性是否配 Saga 编排器；关键写流程是否有补偿/事件。
- 依据：saga 节点、关系 `kind=orchestration`、`consistency`。

**7. 服务粒度适中**
- 既不过细（nano/贫血服务、通信开销大），也不过粗（大泥球、部署不独立）。
- 检查：服务过多且彼此高频调用；或单服务内组件/实体过多、职责庞杂。
- 依据：服务数量与关系密度、组件/实体计数。

**8. 反模式检测**

- **共享数据库**、**分布式单体**（服务多但强同步耦合、需一起部署）、**大泥球**、
  **上帝服务**、**贫血模型**、**缺防腐层(ACL)**、**chatty 通信**。
- 依据：拓扑特征 + `tags`(shared-database/god-service/anemic-model/chatty) + 是否缺 ACL/OHS。

**9. 边界防护与契约（ACL / OHS / Published Language）**
- 上游变化应经防腐层隔离；对外提供开放主机服务与发布语言。
- 检查：与外部系统/上游的集成是否有 ACL；跨上下文是否约定发布语言。
- 依据：`tags` 含 `anti-corruption-layer`/`open-host-service`/`published-language`。

**10. 命名与统一语言一致性**
- 服务/元素命名与限界上下文的统一语言一致，无二义、无技术泄漏到业务命名。
- 依据：name 与 description、限界上下文画布的 Ubiquitous Language。

**11. 可独立部署 / 可测试 / 可演进（物理设计原则）**
- 服务应能独立部署与演进；避免因共享/耦合导致必须一起改一起发。
- 依据：耦合度、共享资源、循环依赖。

**12. 团队边界对齐（Conway's Law）**

- 服务边界宜与团队/所有权对齐。
- 依据：`meta.owner`/团队标注（如有）。

### 6.4 评分建议（供命题参考）
- **表达完整性**：模型是否用足元数据/标签（AI 才有据可依）。
- **审查准确度与可解释性**：问题是否命中、是否给出模型依据。
- **建议质量**：改进/重构建议是否可操作。
- **拆分合理性判断**：对"过细/过粗/共享库/分布式单体/循环依赖"等能否识别。
- **工程实现**：审查服务的实现质量（规则引擎 + LLM 混合等）。

### 6.5 实现提示
- 建议**静态规则 + LLM 双通道**：规则引擎先算拓扑指标（耦合/内聚/循环依赖/共享库）
  给出确定性结论；LLM 基于模型 JSON + 规则结果做语义判断与建议，输出 6.2 的结构化报告。
- 审查入口：`POST /api/projects/{id}/review`（现返回 501，选手在后端实现）。
- 参考：Chris Richardson《微服务模式》、DDD 战略/战术设计、Service Cutter 16 项耦合准则、
  华为 CodeArts Modeling 架构检查规则集。

---

## 七、参考
- microservices.io（微服务模式与反模式）
- 领域驱动设计（战略：限界上下文/上下文映射；战术：聚合/领域事件/CQRS）
- ddd-crew / bounded-context-canvas（限界上下文画布 v5）
- Service Cutter（16 项耦合准则）
