# 微服务拆分设计工具 — 待办任务清单

> 整理时间：2026-08-25
> 范围：bac4-standalone（前端）+ backend（FastAPI）比赛底座

---

## 一、进行中（被打断，需收尾）

### 1. 后端“项目”存档改为整工作台
- **现状**：`Header.jsx` 已改动（`git status` 显示已修改，未提交）。
  - 保存/覆盖：由 `exportModel()`（仅 C4）改为 `getFullState()`（全部视图 + 限界上下文画布）。
  - 打开：新格式（含 `boards`）走 `loadFullState()`，旧格式回退 `importModel()` 做向后兼容。
- **原因**：之前后端只存 C4 架构模型，事件风暴 / 领域故事 / 限界上下文画布的内容保存后会丢失。
- **待做**：`npx vite build` 验证构建 + 手动测试（保存含多视图的项目 → 新建清空 → 重新打开，确认各视图都回来了）。
- **优先级**：高（推荐先完成）

---

## 二、推荐待办

### 2. 更新过时文档
以下文档仍是早期内容，未反映多视图工作台、标准 UML / ER（乌鸦脚）、下钻导航等新能力：
- `notes/微服务拆分设计工具-能力研究与底座设计方案.md`
- `notes/C4四层模型说明.md`
- `验收说明.md`
- `项目介绍以及AI审查赛题介绍.md`

**待做**：补充三阶段工作台（业务分析 / 战略设计 / 架构设计）、UML 类三分栏、ER 乌鸦脚、限界上下文画布、字段底部面板、后端整工作台存档等说明。
- **优先级**：高（推荐）

### 3. 前后端联调环境固化
- **现状**：默认后端端口 8000 被本机另一个不相关工程占用，本次临时把后端起到 **8010**，前端用 `VITE_API_BASE=http://localhost:8010` 启动参数指过去。
- **待做**：在 `bac4-standalone` 增加 `.env`（或 `.env.local`）记录 `VITE_API_BASE`，并在 README 写明后端端口约定，避免每次手动传参。
- **优先级**：中

---

## 三、可选增强

### 4. 为新视图补内置示例
- **现状**：内置示例（在线书店 / 外卖订餐 / 在线教育）仅覆盖 C4 架构视图。
- **待做**：为事件风暴、领域故事、限界上下文画布各加一个内置示例，便于演示与验收。
- **位置**：`bac4-standalone/src/examples/index.js`
- **优先级**：中

---

## 四、代码清理（涉及删除文件，需你确认）

> 按照约定，我不会自行删除文件。以下为“待确认删除 / 清理”清单，请你逐项确认后再处理。

| 项 | 文件 / 位置 | 说明 | 建议 |
|----|------------|------|------|
| 4.1 | `bac4-standalone/src/components/ERMarkers.jsx` | 乌鸦脚早期实现，已被 `ERCrowEdge.jsx` 取代，无其它文件引用（仅自引用） | 删除 |
| 4.2 | `elementTypes.js:140` `boundedContext`（board `context-map`） | 孤立节点类型；实际限界上下文是表单视图 `bounded-context`，此节点无入口 | 移除该类型定义 |
| 4.3 | `App.jsx` `handleNodesChange` 内注释 | annotation 尺寸追踪因死循环被禁用，刷新后尺寸不持久（`TODO: Fix this properly later`） | 评估是否需要，或删掉死代码/注释 |

---

## 五、明确不做（已决定）

- **AI 智能审查逻辑**：属比赛选手实现内容。后端 `POST /api/projects/{id}/review` 返回 501 占位，仅预留元数据 / 标签 / 标准接口。
- **可视化嵌套框**（系统大框套容器小框）：暂缓。

---

## 附：本地运行方式（当前会话）

- 前端：`cd bac4-standalone && VITE_API_BASE=http://localhost:8010 npm run dev` → http://localhost:5175/
- 后端：`cd backend && ./.venv/bin/uvicorn app.main:app --host 127.0.0.1 --port 8010`
- 健康检查：`curl http://localhost:8010/api/health`
