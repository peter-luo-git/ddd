# 验收说明（MVP 第一阶段）

> 一句话：以 BAC4 为骨架的**微服务拆分设计工具**已中文化，扩充了微服务元素与设计元数据/标签标准，内置 3 个示例系统，并新增 Python(FastAPI) 后端（项目存取 / 校验 / 导出 / AI 评审占位）。

## 一、如何启动

### 前端（画布）
```bash
cd bac4-standalone
npm install          # 首次
npm run dev          # 启动后打开 http://localhost:5173 （或终端提示的端口）
```

### 后端（可选，用于"项目"保存/加载）
```bash
cd backend
python3 -m venv .venv                       # 首次
./.venv/bin/pip install -r requirements.txt  # 首次
./.venv/bin/uvicorn app.main:app --port 8000
# 接口文档：http://127.0.0.1:8000/docs
```
> 前端不依赖后端也能用（画布、示例、导入导出、本地自动保存都在浏览器内）。
> 只有点"项目 → 保存/打开"时才需要后端；后端没起时会有中文提示，不影响其它功能。

## 二、验收清单（可逐条勾）

**前端 · 中文化**
- [ ] 顶部标题「微服务拆分设计工具」，菜单全中文（层级/示例/项目/导出/导入/布局/清空）
- [ ] 左侧工具栏中文，分「C4 元素」「微服务基础设施」两组
- [ ] 属性面板中文（元素/关系属性、设计元数据、删除确认等）

**前端 · 微服务元素**
- [ ] 层级切到「容器」后，工具栏出现：API 网关、消息中间件、数据库、文件/对象存储、缓存、Saga 编排器
- [ ] 能拖到画布、能连线、能在属性面板改名/技术栈/描述

**前端 · 设计元数据（AI 评审标准，预留）**
- [ ] 选中元素：可选「子域类型」(核心/支撑/通用)、「是否有状态」
- [ ] 选中关系：可选「通信方式」(同步/异步/发布订阅/…)、「一致性」(强/最终)
- [ ] 这些值随「导出 JSON」一并导出（在元素/关系的 `meta` 字段里）

**前端 · 示例**
- [ ] 点「示例」可加载 3 个系统：在线书店 / 外卖订餐平台 / 在线教育课程平台
- [ ] 加载后画布出现该系统的微服务拆分（服务 + 网关 + 中间件 + 库 + 关系）

**前端 · 导入导出**
- [ ] 导出 JSON / Structurizr / PlantUML / Mermaid / Draw.io / PNG / SVG 正常
- [ ] 导出的 JSON 再导入可还原

**后端（起了后端时）**
- [ ] 「项目 → 保存为新项目」成功；刷新后「项目」列表能看到它并可打开
- [ ] `http://127.0.0.1:8000/docs` 可见全部接口
- [ ] `POST /api/projects/{id}/review` 返回 501（AI 评审占位，符合设计约定）

## 三、本次交付内容

1. **前端全面中文化**（保留 C4/DDD/API Gateway 等专业词）。
2. **底座重构**：新增 `src/config/elementTypes.js` 元素类型注册表 = 单一事实源；store 收敛为单一 `elements` 数组。**以后加一种元素只改这一个文件**。
3. **新增 6 个微服务基础设施元素**：API 网关、消息中间件、数据库、文件/对象存储、缓存、Saga 编排器。
4. **设计元数据/标签标准落地**：元素(子域类型/有状态)、关系(通信方式/一致性)可编辑并导出；受控标签词表内置。**AI 评审逻辑按约定不实现，只预留标准与接口占位。**
5. **3 个示例系统**（中小规模微服务拆分，C4 容器层）。
6. **Python FastAPI 后端**：项目 CRUD（SQLite）、模型校验、导出(JSON/DSL)、词表、AI 评审接口占位(501)。
7. **前后端打通**：Header「项目」菜单存/取后端，后端不可用时优雅降级。

## 四、已测试

- 前端：`npm run build` 通过（1696 模块）、`npx eslint src` 无错误。
- 示例：`node scripts/validate-examples.mjs` 校验 3 个示例结构全部有效。
- 后端：curl 跑通全部接口（健康/词表/CRUD/校验/DSL 导出/评审 501/删除）；CORS 预检通过。

## 五、已知限制 / 后续（留给后续迭代或选手）

- **AI 评审未实现**（按你的要求只预留标准与占位接口）。评审维度规格见
  `notes/微服务拆分设计工具-能力研究与底座设计方案.md` 第 5 节。
- 无 DSL 文本反向编辑（按决策：只导出 DSL 供 AI，不做双向同步）。
- 战略/战术层元素（子域、聚合、领域事件等）暂未进画布，当前聚焦"微服务 + 关系 + 基础设施"。
- 后端为单用户本地版（SQLite），未做鉴权/多人协作。

## 六、相关文档

- 设计蓝本：`notes/微服务拆分设计工具-能力研究与底座设计方案.md`
- 前端说明：`bac4-standalone/README.md`（原 BAC4 文档）
- 后端说明：`backend/README.md`
